require("project_nvim").setup({
  -- patterns = { ".git", "pyproject.toml", "src", "package.json" },
  -- sync_root_with_cwd = true,
  -- respect_buf_cwd = true,
  -- update_focused_file = {
  --   enable = true,
  --   update_root = true
  -- },
})
